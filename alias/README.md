# yf-log

Unscoped alias of [`@yourfam/yf-log`](https://www.npmjs.com/package/@yourfam/yf-log). Same command, same version.

```bash
npx yf-log --help
```

Install the scoped package if you can; this name exists so `npx yf-log` cannot be taken by someone else.
